# Packages built for aarch64 against Armtix and published as a pacman repo.
#
#   omarchy NAME          pkgbuilds/NAME from omacom/omarchy-pkgs (branch master,
#                         latest at build time -- omarchy's own recipes, which
#                         already declare aarch64; they just never build it)
#   aur NAME COMMIT       an AUR package pinned to one commit of its git repo.
#                         Pinned because AUR recipes are user-submitted and move;
#                         bump the commit deliberately, after reading the diff.
#
# Only what the ARM boxes cannot get elsewhere: not in Armtix, not in omarchy's
# aarch64 repo, not an arch-independent package from its x86_64 repo.
omarchy asdcontrol
omarchy tzupdate
omarchy omasnap
omarchy omacalc
omarchy omacut
omarchy omawrite
